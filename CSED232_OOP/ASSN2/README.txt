<Assignment2 README FILE - 20220124 김문겸>

list.h : list class를 선언한 헤더파일이다. node.h를 헤더파일로 가진다.
- node들을 연결한 list를 관리하기 위한 다양한 함수를 선언하였다.

node.h : node class를 선언한 헤더파일이다. student.h를 헤더파일로 가진다.

student.h : student class를 선언한 헤더파일이다.
- student의 정보를 입력받거나, 정보를 보여주는 함수들이 존재한다.

student.cpp : student.h에서 선언된 함수를 정의하는 파일이다. student.h를 헤더파일로 가진다.

list.cpp : list.h에서 선언된 함수를 정의하는 파일이다. list.h를 헤더파일로 가진다.

assignment2.cpp : main함수가 존재하는 파일이다. student.h, list.h를 헤더파일로 가진다.
